"use strict";
/* import {model as bookdb} from '../models/schema'

// Method : GET  function to respond with all books
 async function getAllBooks(request:any,response:any){
    try{
        const books=await bookdb.find();
        response.send(books)
    }
    catch(error)
    {
        console.log(error.message)
    }
}

export {getAllBooks} */ 
